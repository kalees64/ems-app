import LeavesList from "@/components/LeavesList";

import React from "react";

const LeaveType = () => {
  return (
    <section>
      <LeavesList />
    </section>
  );
};

export default LeaveType;
